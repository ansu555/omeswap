'use client';

export default function AlgorandWalletConnect({ variant }: { variant: string }) {
  return (
    <button className="px-4 py-2 rounded-lg bg-primary text-white hover:opacity-90 transition-opacity">
      Connect Wallet
    </button>
  );
}
